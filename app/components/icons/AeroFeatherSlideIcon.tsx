export function AeroFeatherSlideIcon({ className, alt }: { className?: string; alt?: string }) {
  return (
    <img
      src="/assets/aero side profile for feather slide.svg"
      alt={alt ?? "Aero side profile"}
      className={className}
      loading="lazy"
    />
  );
}
