export function AeroNavigationSlideIcon({ className, alt }: { className?: string; alt?: string }) {
  return (
    <img
      src="/assets/aero side view for navigation slide.svg"
      alt={alt ?? "Aero side view"}
      className={className}
      loading="lazy"
    />
  );
}
