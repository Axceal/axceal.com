export function AeroSenseSlideIcon({ className, alt }: { className?: string; alt?: string }) {
  return (
    <img
      src="/assets/aero inside view for sense slide.svg"
      alt={alt ?? "Aero inside view"}
      className={className}
      loading="lazy"
    />
  );
}
