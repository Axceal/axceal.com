export function AeroCuesSlideIcon({ className, alt }: { className?: string; alt?: string }) {
  return (
    <img
      src="/assets/aero top pic for cues slide.svg"
      alt={alt ?? "Aero top view"}
      className={className}
      loading="lazy"
    />
  );
}
