export function AeroBatterySlideIcon({ className, alt }: { className?: string; alt?: string }) {
  return (
    <img
      src="/assets/aero side view for battary slide.svg"
      alt={alt ?? "Aero battery view"}
      className={className}
      loading="lazy"
    />
  );
}
